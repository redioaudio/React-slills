const getAverage = () => {
    console.log("평균 값을 계산중");

    if ((SVGAnimatedNumberList, length === 0)) return 0;

    const sum = SVGAnimatedNumberList.reduce((acc, cur) => acc + cur);
};

function memo() {
    //useMemo
    //useMemo를 사용하면 함수 컴포넌트 내부에서 발생하는 연산을 최적화 할수이/ㅅ다
    // 먼저 리스트에 숫자를 추가하면 추가된 숫자들의 평균을 보여주는 함수 컴포넌트를 작성해 봅시다

    return <div>memo</div>;
}

export default memo;
